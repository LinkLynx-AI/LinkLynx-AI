import type { Guild, GuildMember, Role } from "@/shared/model/types/server";

export const mockServers: Guild[] = [];

export const mockRoles: Record<string, Role[]> = {};

export const mockMembers: Record<string, GuildMember[]> = {};
