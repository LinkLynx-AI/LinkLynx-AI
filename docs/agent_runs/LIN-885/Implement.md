# Implement.md (Runbook)

- Follow `Plan.md` as the single execution order. If order changes are needed, document the reason in `Documentation.md`.
- Keep the diff limited to LIN-885 scope: user settings route entry, route shell, and regression tests only.
- Preserve the existing `server-settings` modal behavior.
- Run validation after each milestone and fix failures immediately before continuing.
- After each milestone, update `Documentation.md` with decisions and validation evidence.
- If any additional scope is needed, stop and get user approval before expanding.
