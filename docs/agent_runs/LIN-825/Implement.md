# Implement

- `Plan.md` の順に進める。順序を変える場合は `Documentation.md` に理由を残す。
- 差分は `LIN-825` の command API に限定し、WS/FE の処理は追加しない。
- `expectedVersion` と tombstone の振る舞いは test で固定する。
- validation は milestone ごとに実行し、失敗したまま次へ進まない。
- `Documentation.md` に decisions / validation / review evidence を随時追記する。
