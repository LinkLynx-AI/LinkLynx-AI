# LIN-642 Implement Runbook

- Plan.md の順序で実装する。順序変更時は理由を Documentation.md に記録する。
- 差分はLIN-642スコープに限定し、無関係な改善を混ぜない。
- 保護ルートは「preview query 指定時は強制表示、通常時は実認証判定」の順で判定する。
- `returnTo` は内部保護ルートのみに正規化し、不正値は破棄する。
- 401は `/login` 誘導、403/503はガード画面表示で契約を固定する。
- 各マイルストーン完了時にテストを実行し、失敗は即修正してから次へ進む。
