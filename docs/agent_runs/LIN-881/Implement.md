# Implement.md (Runbook)

- Follow `Plan.md` in order: gather facts, audit behavior, run targeted validations, and record findings.
- Do not patch application code in this run.
- If a validation command fails due to an environmental issue, record the command and failure in `Documentation.md`.
- Keep the result decision-oriented: each finding must map back to a `LIN-881` acceptance criterion or ADR-004 rule.
