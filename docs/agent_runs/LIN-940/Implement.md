# Implement.md (Runbook)

- Follow `docs/agent_runs/LIN-940/Plan.md` as the single execution order.
- Keep the parent integration diff scoped to the remaining `LIN-944` regression and documentation evidence only.
- Run review and validation before opening the `main` PR.
- Record PR URL, base branch, and human-review stop condition in `Documentation.md`.
