# Implement.md (Runbook)

- `LIN-942` は backend のみを扱い、frontend/API client の接続変更は入れない。
- `LIN-941` で決めた `guild_category` / `parent_id` / `position` 契約を唯一の前提にする。
- validation と未実装の frontend handoff を `Documentation.md` に残す。
