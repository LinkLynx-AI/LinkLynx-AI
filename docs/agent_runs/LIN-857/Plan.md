# LIN-857 Plan

## Milestones
1. 削除対象（v0権限資産）を migration で定義する。
2. down migration で旧構造の空再作成方針を実装する。
3. seedをv2権限モデルへ更新する。
4. DATABASE/契約文書の現行状態を更新する。
5. `db-migrate` / `db-schema` / `db-schema-check` / `gen` / `validate` を実行する。
