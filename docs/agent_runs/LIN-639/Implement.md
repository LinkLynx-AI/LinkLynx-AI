# LIN-639 Implement Rules

- Keep changes limited to env contract, startup validation, compose wiring, and docs.
- Preserve existing auth REST/WS response contract.
- Do not add secret values to tracked files.
- Prefer explicit validation errors over silent fallback for startup contract checks.
- Avoid unrelated refactors.
