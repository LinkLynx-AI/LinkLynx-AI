# Implement.md (Runbook)

- Follow Plan.md as the execution order.
- Keep the diff limited to staging smoke deploy baseline and supporting docs.
- Reuse the existing `rust_api_smoke_deploy` module instead of creating a parallel implementation.
- Record any branch / PR stacking decisions in `Documentation.md`.
