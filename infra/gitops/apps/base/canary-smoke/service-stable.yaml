apiVersion: v1
kind: Service
metadata:
  name: canary-smoke-stable
  labels:
    app.kubernetes.io/name: canary-smoke
    app.kubernetes.io/part-of: linklynx-platform
spec:
  selector:
    app.kubernetes.io/name: canary-smoke
  ports:
    - name: http
      port: 80
      targetPort: http
